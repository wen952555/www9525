services:
  - type: web
    name: vpn-subscription-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app.main:app --host 0.0.0.0 --port 8000"
    plan: free
    envVars:
      - key: DATABASE_URL
        value: <YOUR_RENDER_POSTGRESQL_URL>